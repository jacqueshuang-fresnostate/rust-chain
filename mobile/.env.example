# 浏览器开发始终请求 Vite 同源地址；该变量只决定 Vite 把 HTTP/WebSocket 转发到哪里。
# 例如真实测试服务运行在 18080 时改为 http://127.0.0.1:18080，不会让浏览器改为跨域直连。
VITE_BACKEND_DEV_PROXY_TARGET=http://127.0.0.1:8080

# 可选的浏览器/原生直连后端 origin。公开 PWA 留空时使用页面同源反向代理。
# Tauri 发布构建必须注入设备可访问的 HTTPS origin，不能使用 localhost 或 127.0.0.1。
VITE_BACKEND_API_DOMAIN=
VITE_BACKEND_API_PREFIX=/api/v1

# PWA 的永久部署根路径。独立域名使用 /，子路径部署示例为 /mobile/。
# Tauri 构建会忽略该值并继续使用原生根路径。
VITE_PWA_BASE=/
