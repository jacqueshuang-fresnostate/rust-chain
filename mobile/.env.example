# 本地开发代理目标；公开 PWA 必须覆盖为 HTTPS 后端或使用同源反向代理。
# iOS 与 Android 发布构建同样应通过构建环境注入真实后端。
VITE_BACKEND_API_DOMAIN=http://127.0.0.1:8080
VITE_BACKEND_API_PREFIX=/api/v1

# PWA 的永久部署根路径。独立域名使用 /，子路径部署示例为 /mobile/。
# Tauri 构建会忽略该值并继续使用原生根路径。
VITE_PWA_BASE=/
