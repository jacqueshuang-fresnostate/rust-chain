# Pencil 当前选中根导航与交易选择弹窗规格

## 来源

- Pencil 文件：`/private/tmp/hippo-mobile-uiux-position-tab.pen`，仅通过 Pencil MCP 读取。
- 当前选中节点：`X0ux9F`，名称“首页 / 01-2 / 访客状态 / 交易导航选择弹窗 / 浅色主题”。
- 画板：390×1144px，背景 `#F7F9F8`。

## 根 Dock

| 节点 | ID | 规格 |
| --- | --- | --- |
| 导航外层 | `lsATG` | 390×84px，padding `[6,16,10,16]` |
| Dock | `XV7iv` | 358×68px，白色，24px 圆角，`#CCD5D0` 1px inner stroke，shadow `0 8 24 #07110D14` |
| 中央交易球 | `t1KVLE` | 56×56px，x151/y-12，`#43EFA9`，28px 圆角，shadow `0 6 16 #43EFA966` |

- Dock 水平内边距 16px，五栏结构。
- 普通栏目 icon 22px、标签 10px、图文 gap 4px。
- 中央图标为 Lucide `arrow-left-right` 24px，颜色 `#07110D`。
- 普通选中图标 `#087B52`、标签 `#111714`；未选中 `#68736D`。

## 遮罩

- `RtubA`：整画板绝对覆盖，390×1144px，颜色 `#00000059`。
- 生产必须用 fixed viewport 覆盖并 Teleport 到 body，避免应用舞台坐标系影响。

## 特殊轮廓

- `U99rP`：x16/y762，358×340px，clip true；生产只需让 300px 路径与 54px 关闭按钮共同占据该底部区域。
- `n1BXc`：358×300px，白色，shadow `0 6 22 #0000001A`。
- 原始 path：

```text
M26 0h306c14.36 0 26 11.64 26 26v252c0 12.15-9.85 22-22 22h-112c-6 0-10.5-4-11.5-10-3-18.5-17-32-33.5-32-16.5 0-30.5 13.5-33.5 32-1 6-5.5 10-11.5 10h-112c-12.15 0-22-9.85-22-22v-252c0-14.36 11.64-26 26-26z
```

- viewBox：`0 0 358 300`。
- 画板中路径底部 y1062、Dock 顶部 y1066，相距 4px；生产固定底部应为 `82px + safe-area`。

## 四行内容

- `eLvdo`：x30/y774，330×256px；相对轮廓 x14/y12；垂直 gap 4px。
- 每行 330×58px、12px 圆角、padding `[0,18]`、图文 gap 16px。
- 图标 25px，Geist 18px/600。
- Pencil 原稿包含当前项 `#F4F4F4` 背景、650 字重和 24×24px 圆形勾选标记；根据最终产品反馈，生产实现全部省略，不渲染 `active` 状态。
- 生产业务映射：现货 `RefreshCw`、合约 `CandlestickChart`、秒合约 `Zap`、闪兑 `ArrowDownUp`。

## 关闭按钮

- `QrlAB`：x168/y1055，54×54px，`#07110D`，27px 圆角，3px 白色 stroke，shadow `0 6 16 #00000045`。
- Lucide `X` 28px、白色。
- 距画板底部 35px；生产固定 bottom 为 `35px + safe-area`。

## 生产边界

- Pencil 决定轮廓、尺寸、间距、层级与动效起止位置；路由能力决定四行真实业务文案和目标。
- 不把 Pencil 中无生产能力的“策略交易/期权”变成假按钮。
- 保留现货/合约最近交易对与模式，秒合约来源状态，以及根栏目 replace 语义。
- 明暗主题、320–448px 流式宽度、安全区、键盘焦点和 reduced-motion 必须作为生产增强保留。
