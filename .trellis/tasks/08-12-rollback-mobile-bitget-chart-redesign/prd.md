# 回滚 Bitget 风格手机现货 K 线重构

## Goal

按用户反馈撤销本轮 Bitget 风格手机端现货 K 线重构，恢复到功能提交 `f27032a` 之前的手机行情详情页面和相关测试、规范及任务记录。

## Requirements

- 撤销 `f27032a` 引入的全部生产代码、i18n、样式、测试、Mobile 规范和进度记录。
- 同步撤销本轮任务归档提交 `04997c7` 与会话记录提交 `1547158`，避免留下已被用户否决的任务记录。
- 保留 `f27032a` 之前已经完成的 Bitget 现货最新价统一、实时行情、双本地图表引擎及其他无关功能。
- 使用 `git revert` 生成可追溯回滚，不重写 Git 历史。

## Acceptance Criteria

- [x] `mobile/src/views/MarketDetailView.vue` 与 `f27032a^` 中的版本一致。
- [x] 本次被否决的四页签、大尺寸 376px 图表和交易/图表模式栏已移除。
- [x] Mobile 全量测试、类型检查和 PWA 构建通过。
- [x] `git diff --check` 通过，工作树最终干净。

## Out of Scope

- 不重新设计行情详情页。
- 不回滚 `b486d17` 的 Bitget 现货价格权威与实时行情修复。
- 不修改后端接口或其他手机页面。
